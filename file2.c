#include <stdio.h>
int main() {
    int a, b, sum;
    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b); // Takes input from the user
    sum = a + b;
    printf("Sum = %d", sum); // Prints the sum
    return 0;
}

